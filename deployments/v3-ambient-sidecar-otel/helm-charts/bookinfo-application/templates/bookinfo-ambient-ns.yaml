apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.app.ambient.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    istio-discovery: enabled
    istio.io/dataplane-mode: ambient

