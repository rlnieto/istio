minio:
  name: minio
  namespace: tracing-system
  # Minio desplegado en el mismo cluster
  endpoint: http://minio.minio.svc.cluster.local:9000
  bucket: tempo
  access_key_id: adminuser
  access_key_secret: adminuser