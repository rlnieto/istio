apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.istioIngressGateway.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    istio-discovery: enabled
    istio-injection: enabled
    istio.io/dataplane-mode: ambient
